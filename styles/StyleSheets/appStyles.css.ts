import { style } from '@vanilla-extract/css';

export const Title = style({
  color: 'blue',
});

export const centerText = style({
  textAlign: 'center',
  margin: 'auto',
});

export const tabulaRasa = style({
  fontSize: 20,
  backgroundColor: 'beige',
  width: '300px;',
  margin: 'auto',
  padding: '10px',
});
